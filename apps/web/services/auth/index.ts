// Auth provider: Clerk (cross-app standard, locked 2026-06-04).
// Use @clerk/nextjs helpers (currentUser, auth, ClerkProvider, useUser, etc.)
// for all session and identity work in this app.
export function authServicePlaceholder() {
  // TODO: Implement production auth/session orchestration after Sprint Zero.
  return "auth-service-placeholder";
}