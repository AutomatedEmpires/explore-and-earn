# App Router Scaffold

This directory is intentionally limited to Sprint Zero placeholders.

Route groups are created to match the canonical Notion structure:

- `(marketing)`
- `(public)`
- `(seeker)`
- `(host)`
- `(admin)`
- `(demo)`
- `api`

TODO: Add real route files only after the corresponding product surfaces,
permissions, contracts, and lifecycle rules are approved for implementation.