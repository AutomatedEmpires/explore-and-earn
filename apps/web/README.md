# apps/web (placeholder)

The Explore&Earn web app. **Not implemented yet** — Sprint Zero establishes the control plane only.

- Proposed framework: **Next.js (App Router)** — https://nextjs.org/docs (pending confirmation).
- Consumes the design system from `packages/ui` (tokens, primitives, icons, DiscoveryCard).
- Mobile-first, card-first. See `docs/product/product-principles.md`.

## Not in scope yet

No auth, no DB schema, no billing, no matching, no real marketplace flows. First feature build pack will be the **Discovery Card** in `packages/ui`, then a discovery feed surface here — each via a scoped, approved PR.
