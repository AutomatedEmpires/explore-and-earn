# tools/ (placeholder)

Repo tooling, scripts, and codegen. **Empty during Sprint Zero.**

Future candidates: token export/codegen from `docs/design/design-system-v1.md`, icon registry validation, drift-guardrail scripts (shared with CI), Notion<->repo sync helpers. Add via scoped PRs.
