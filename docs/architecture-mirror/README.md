# Architecture Mirror

Offline, build-time mirrors of canonical Notion / design source-of-truth pages,
for implementation reference. The Notion / `docs/design` canon always wins; these
files describe how canon is *implemented* in code.

## Index

- `design-tokens.md` — locked V1 design tokens (`apps/web/styles/tokens.css` +
  `packages/ui/src/tokens.ts`).
- `icon-registry.md` — Streamline Freehand icon registry (`packages/ui/src/icons`).
