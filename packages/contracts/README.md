# packages/contracts (placeholder)

Shared types, schemas (e.g. zod), and API contracts used across `apps/web`, `packages/ui`, and `packages/db`. **Not implemented yet.**

No real domain schema lands here during Sprint Zero — object modeling and the data dictionary are tracked in Notion canon and will arrive via a scoped, approved build pack.
