# packages/db (placeholder)

Database access layer + generated types. **Not implemented yet.**

> Founder approval gate: database schema, migrations, and any destructive operations require explicit approval (see `docs/agents/founder-approval-gates.md`). Sprint Zero ships **no** schema, **no** migrations, and **no** Supabase client wiring.
